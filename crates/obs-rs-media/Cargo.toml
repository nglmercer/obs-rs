[package]
name = "obs-rs-media"
description = "Portable media values and the reference RGBA frame model for OBS-RS"
edition.workspace = true
publish.workspace = true
version.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
rayon = "1.12.0"
