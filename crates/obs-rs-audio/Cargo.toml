[package]
name = "obs-rs-audio"
description = "Safe audio buffers, bounded queues, and reference mixer for OBS-RS"
edition.workspace = true
publish.workspace = true
version.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
obs-rs-media = { path = "../obs-rs-media" }
