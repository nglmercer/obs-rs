[package]
name = "obs-rs-diagnostics"
description = "Bounded deterministic diagnostics and recovery bundles for OBS-RS"
edition.workspace = true
publish.workspace = true
version.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
