[package]
name = "obs-rs-render"
description = "Portable Rust render-backend contracts and deterministic CPU fallback for OBS-RS"
edition.workspace = true
publish.workspace = true
version.workspace = true

[lib]
crate-type = ["lib"]

[dependencies]
obs-rs-media = { path = "../obs-rs-media" }
