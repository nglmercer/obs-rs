# Known bugs and evidence gaps

**Baseline date:** 2026-08-20  
**Reference:** OBS Studio 32.2.2  
**Rule:** A failure is recorded here even when it may be environment-specific;
it must be reproduced or explicitly classified before the coordinator closes it.

| ID | Type | Observed behavior | Impact | Reproduction / evidence | Next owner / disposition |
| --- | --- | --- | --- | --- | --- |
| BUG-001 | Quality gate | Resolved: strict Clippy previously failed with `-D warnings` in existing project/benchmark/UI code. | The required verification command is now clean, so new phase work cannot hide behind baseline lint drift. | `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes after documentation, formatting, and narrow lint fixes. | Coordinator: keep the strict gate in every phase packet. |
| BUG-002 | Production output / environment | Native GStreamer production stream fixtures need a reachable local sink and currently fail their state transition in this managed session. | Production RTMP/RTMPS/SRT startup remains unverified here; the product still returns a typed native error rather than silently falling back. | The two tests are explicitly ignored with a local-sink requirement; reference/native output tests remain available for a compositor/stream-hosted run. | Output Agent + Reviewer: run on a real sink, distinguish missing plugin/sink from a pipeline bug, and add capability-aware live evidence. |
| BUG-003 | Test environment / GUI | One GUI test cannot initialize the winit backend because no Wayland compositor is available. | The test is explicitly ignored in this managed session; the native window lifecycle remains unverified here. | `cargo test -p obs-rs-gui --bin obs-rs-gui -- --test-threads=1`: 104 pass, one compositor-dependent test and one timing probe are ignored. | QA Agent: provide a compositor-backed CI job or retain the explicit environment classification. |
| BUG-004 | Test environment / capture | X11 and live window tests cannot connect; PipeWire discovery exits 255; no camera is present. | Platform capture/audio parity is unverified on this host. | `obs-rs-linux-check` reports X11/window/camera/PipeWire skip; several live tests are explicitly ignored. | Platform Agents: run the same probes on real X11, Wayland/PipeWire, and device fixtures. |
| BUG-005 | Performance | The release benchmark reports 120/120 single-worker deadline misses and 60/60 multi-worker misses. | The current timing baseline cannot be used as a 60 FPS acceptance result. | `cargo run -p obs-rs-app --bin obs-rs-benchmark --release`; see `PERFORMANCE-BASELINE.md`. | Performance Agent: profile scheduler wait/lateness and separate container scheduling noise from product latency. |
| BUG-006 | Performance architecture | Resolved for viewport sizing: GUI preview now composes into a bounded aspect-preserving target before the explicit CPU/Slint compatibility bridge. | A 4K canvas no longer requires a full-resolution GUI readback merely to fill the preview viewport. A true native/WGPU Slint presenter is still absent. | WGPU 8x8->4x4 target test, GUI 1920x1080->1048x590 format test, and `PreviewPresenter` boundary. | GPU Agent + UI Agent: replace the compatibility bridge when Slint/native-surface integration is ready and retain byte-copy metrics. |
| BUG-007 | Canvas behavior | Fixed presets, continuous cursor-anchored wheel zoom, middle-button pan, Space+drag pan, bounded snapping, EBU/ITU safe-area snap edges, persisted safe-area guide visibility, Alt-crop, multi-selection, group transforms, arrow-key nudge, Transform-menu alignment, persisted/rendered rotation, and selected-hit-stack traversal now use Rust-owned state. Live pointer/DPI evidence and exact guide styling remain absent. | Core viewport, grouped transform, keyboard nudge, command-menu behavior, anchored wheel zoom, plain-click selection of an obscured source, safe-area snap geometry, and the editable-preview guide overlay work, while full OBS canvas editing parity remains unavailable. | Media/project/WGPU rotation oracles, crop/snap/safe-area/group/command geometry tests, selected-hit traversal and zoom-anchor invariants, project batch-transform test, settings round-trip/runtime propagation, localized settings-page rendering, and GUI suite; no compositor-backed pointer fixture or live-DPI zoom test exists yet. | Canvas Agent: complete interaction fixtures and the visual reference comparison. |
| BUG-008 | Canvas behavior | EBU/ITU safe-area snapping and the persisted Show safe areas overlay are now part of the bounded canvas policy; ordinary resize preserves aspect, Shift enables free resize, Ctrl disables snapping, arrow movement is 1px or Shift+10px, rotated Alt-crop maps pointer deltas through source-local axes, and the Transform menu covers screen sizing/centering/alignment. | Transform editing remains a grouped single-canvas subset of OBS behavior; plain preview clicks skip already-selected overlapping hits, while exact guide styling, rotated visual handle orientation, fine-resize semantics, and live pointer/DPI behavior remain unverified. | `CANVAS-003`/`CANVAS-006` rows, selected-hit traversal and `obs-rs-ui` selection tests, canvas modifier/geometry/safe-area/rotated-crop tests, settings persistence/runtime tests, localized settings-page rendering, and GUI keyboard/menu coverage document the implemented subset. | Canvas Agent: add compositor-backed rotated-handle/pointer fixtures and compare guide styling one packet at a time. |
| BUG-009 | Dock behavior | A bounded `DockNode` split/tab tree owns validated persistence; pane and splitter projections render loaded layouts, header drags show bounded tab/directional insertion targets, and detached windows persist/reopen scale-aware geometry. Restored positions use the shared virtual-desktop bounds when the platform can enumerate monitors and keep a 48px visible strip. | OS monitor identity/per-monitor selection and compositor-backed multi-monitor workflows remain unverified; platforms without monitor bounds preserve coordinates without a clamp; custom/plugin docks are not in the built-in catalog. | `crates/obs-rs-gui/src/dock_tree.rs` validates drop/resize mutations; `ui/dock_workspace.slint` renders split handles and indicators; settings round-trips geometry; dock pure tests cover off-screen, negative-secondary-monitor, and oversized-window restoration. | Platform/QA Agents: add monitor capability metadata and live multi-monitor/DPI fixtures. |
| BUG-010 | Source parity | Repeated references, nested scenes, and the first embedded-group slice are implemented: project scene items, recursively ordered group children, path-addressed visibility/locking/order/removal/duplicate/transform/copy-paste commands, Sources-dock group-child rows, stable runtime scene-item identities, engine startup, and preview sync preserve shared captures; nested targets persist with cycle validation and flatten through axis-aligned scale/translation/opacity. Root-level source duplication now selects the fresh item after the typed command completes. | Duplicate-reference, nested-scene, and basic nested-group workflows have runtime coverage, and the Sources dock can display nested children and edit their visibility, locking, local order, removal, reference/source-clone duplication, target-aware copy/paste, safe direct Transform-menu actions, source-definition filters, source properties, and modal transforms for nested non-group rows. Nested crop/rotation/flip semantics at transformed group boundaries and independent editing of nested descendants remain incomplete; nested monitor-picker workflows are deliberately unavailable until they have a target-aware platform path. | Core duplicate-instance compositor and stable-ID addressing/layer tests; project nested/group round-trip, recursive duplicate-source, cycle, depth-bound, group-child command, group-child removal, group-child duplicate-mode, nested-transform validation, nested copy/paste, and stable-path flattening tests; media composition test; engine and GUI nested/group-render tests; GUI Sources-dock projection/callback, nested filter/properties/transform-window, root duplicate-selection, and callback tests; `scene_sources` retains shared references and capture latency records one physical source render. | Engine/Scene Agent: complete nested child UI/commands, target-aware monitor workflows, and full nested transform/editing semantics. |
| BUG-011 | Filter parity | Project filter kinds/settings can persist even when runtime compilation does not produce an implementation. | `compile_filter` now activates grayscale, brightness, opacity, Crop/Pad edge clearing, the six-control fixed-point Color Correction slice, bounded RGB-distance Color Key, bounded four-control Luma Key, bounded current-pixel YCbCr Chroma Key core, bounded 3x3 Sharpen core, RGB Color Multiply/Add color wash, timestamp-driven Scroll with strict speed/loop validation, and bounded 0..500 ms Render Delay. Render Delay is owned by the shared source runtime, warms up with no early current-frame leak, resets on source/timestamp discontinuity, and reports its frame/byte-capacity failure rather than silently degrading. The audio graph now applies bounded OBS Gain, Invert Polarity, stateful Limiter, stateful own-signal Compressor, stateful peak Expander, and a stateful peak Noise Gate with open/close threshold and attack/hold/release controls to live desktop/microphone channels through an ordered fixed-capacity chain. Noise Suppression is intentionally not offered in the Add Filter menu until its Speex/RNNoise native capability boundary is implemented. Unknown audio/video filters, HDR, and the remaining video/audio filter kinds still have no runtime operation and are omitted. | Media/audio CPU tests, engine/project compiler and legacy conversion tests, GUI filter-property/workflow tests, timing evidence, and WGPU submission/readback tolerance tests cover the activated slices; Render Delay's GPU-native texture history, multiple temporal filters, async 20-second video delay, high-resolution histories over the 256 MiB CPU budget, and arbitrary ordered temporal/pixel-chain semantics remain unverified. Scroll's width/height limits and arbitrary ordered-chain behavior, OBS Chroma Key box filtering/color-space negotiation/optional controls, Color Correction color-property grouping and color-space behavior, Sharpen transformed or chained-neighbour semantics, Compressor/Expander/Gate sidechain and detector behavior, RMS/knee/upward-compressor modes, Noise Suppression, full filter-graph integration, project-to-audio-source routing, and complete filter persistence for every kind also remain unverified. The CPU Scroll oracle intentionally takes a copy-on-write source snapshot, so high-resolution real-time use still requires reusable scratch or GPU-only routing. | Filter Agent: typed filter graph must return explicit unavailable capability, never silently omit. |
| BUG-015 | Source parity | Bounded portable image, timestamp-driven slideshow, and bitmap-text sources are now available with live settings updates. Images support PNG/JPEG/GIF/WebP/PNM decode, target-frame resize, decoder limits, and last-good-frame retention; the slideshow supports bounded file/directory expansion, name ordering, opt-in randomized ordering, sequential timestamp selection, loop/hold behavior, and atomic reload; text intentionally supports only a built-in 5x7 glyph map. | Static image/slideshow/text workflows work deterministically, while OBS manual/hotkey playback, fade/swipe/slide transitions, file-watch reload, media playlist, media/browser/VLC playback, font selection, Unicode shaping, rich layout, outlines, and scrolling remain unavailable. | `obs-rs-builtins` image decode/limit/atomic-update, slideshow timestamp/directory/randomization/limit/failed-reload, and text create/render/update/rejection tests; GUI typed-property/Add Source fixtures cover the three portable slices. | Source Agent: add resource-backed font/layout and media-clock/transition boundaries, then implement animated/media/browser/VLC sources one packet at a time. |
| BUG-016 | Output parity | A bounded packetized replay buffer retains encoded history by byte/time limits and saved snapshots begin at a retained video keyframe. | The saved artifact is the inspectable `OBSRPKT1` reference container; the output layer has bounded keyframe-aware split writers with atomic publication, known-artifact cleanup, and synchronous/nonblocking `EngineSession`/`EngineWorker` APIs for reference and supported native production containers. Settings > Output persists bounded split enable/duration/size/count controls and routes production starts through the worker. Native finalization rejects empty artifacts and cannot clobber an existing final path. Worker-facing replay requests are bounded/nonblocking, publish lifecycle/save status, and the Controls dock exposes a persisted 1–3,600 second / 1–256 MiB history. Save Replay plus Start/Stop Replay have persisted typed local hotkeys. Automatic production remux is implemented for unsplit H.264/AAC Matroska recordings: the native boundary keeps `.mkv.part`, `.mp4.part`, and a matching bounded JSON sidecar hidden until MP4 publication, the GUI setting is capability-gated and nonblocking, and Controls performs bounded asynchronous candidate discovery before presenting a chooser for exact-path recovery. GUI startup now queues one bounded scan of the configured recording directory and uses the same explicit chooser when marked candidates exist; it never silently remuxes or overwrites a destination. Replay remux, split/remux combinations, and global hotkey registration remain incomplete. | `obs-rs-output` replay and segmented-writer tests; engine and worker split/remux/recovery/candidate-discovery lifecycle/publication tests; output-gstreamer native stale-artifact, splitmux, remux, recovery, manifest atomicity/matching, candidate-scan, no-clobber, and hidden-artifact tests; settings persistence and GUI output-policy/automatic-remux/recovery/candidate-discovery/startup-scan fixtures; GUI Controls-dock asynchronous recovery fixture; toolkit/frontend shortcut action/code tests. | Output Agent: complete replay/remux variants and the remaining global/configurable desktop actions. |
| BUG-017 | Studio multiview | View mode 2 renders a bounded 16-scene thumbnail composite from the single preview worker, supports preview/program selection from tile clicks, and projects one output-status strip with global frame/drop/audio-block/queue counters plus a mixed-audio dB meter. | Per-scene audio meters, source-specific tile controls, fullscreen multiview/projector behavior, and exact OBS tile styling remain incomplete. The program projector itself now enters fullscreen; multiview projector selection is still absent. | GUI render-demand/grid/format/worker/composite/timing/output-telemetry tests and a rendered multiview snapshot; no compositor-backed visual comparison yet. | Studio/UI Agent: add per-scene audio/status semantics and reference visual fixtures without creating a second runtime or unbounded per-scene render path. |
| BUG-012 | Evidence gap | Live screenshots of the main window, Studio Mode, docks, dialogs, menus, projectors, and source windows were not captured. | Visual parity is not yet measured against OBS 32.2.2. | `import -window root` cannot open X server; native GUI test cannot find a Wayland compositor. Settings-page fixtures are available under `artifacts/baseline/screenshots/`. | QA Agent: capture reference/OBS-RS screenshot fixtures on a compositor-backed host. |
| BUG-013 | Build warning | Resolved: the non-GStreamer `push_raw_video` branch now explicitly consumes its conditional argument. | Release benchmark runs without that warning. | `cargo run -p obs-rs-app --bin obs-rs-benchmark --release`. | Coordinator: keep feature-conditional branches warning-free. |
| BUG-014 | Test environment / networking | The WebSocket transport integration fixture cannot bind a local TCP socket in this managed sandbox. | The packet framing test is explicitly ignored here; transport behavior remains covered by non-listening and parser tests. | `cargo test -p obs-rs-output --lib`: 37 pass, 1 ignored with a local-socket requirement. | QA Agent: run the fixture in a network-enabled test job. |

Environment-limited entries are not automatically product defects, but they stay
open until a real platform run supplies evidence. Product parity must not be
marked complete merely because a test was skipped.
